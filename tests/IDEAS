
# PLAN TO DEVELOP TEST

## SIGNAL [2]
- class methods
- format / size

write a _test_(self) method to incorporate tests in the signal class?

## COMPUTING WORKFLOW
We create some template algorithms that perform simple operations, 
but with notable output format [3]

- Algorithm class with its methods [4]
- Format of the results at each step. [4]
- Segmenters, fmap, for each type of algorithm output format [5]

## ALGORITHMS [6]
In general, we dont want to test if an algorithm is correct. 
This is left to the algorithm developer (we provide synthetic signals generators).
- Results of fundamental algorithm classes, with predictable results
- Format of the results --> not requires values

## OTHER ALGORITHMS [8]
- Test fundamental estimators

## SYNTHETIC SIGNAL GENERATOR
Used in the tests, but can be used by the developer 
to generate signals with known characteristics
Generate signals of different sizes, possibly with a smart use of noise.

- standard/fundamental signals [1]
  - zeros
  - ones
  - deltas
  - sinusoids
  - noise 
  - boxcar
  - ...
  Also provide methods to compose fundamental signals
  (e.g. append, sum, scale, change sampling freq)
  
- quasi-physiological synthetic signals: better to just provide some IRF [7]
  so the user will be able to create its own signal (with convolve)


## Development Plan
1. write generators of fundamental signals 
2. extensively test Signal class 
3. write template algorithm classes that perform dumb (predictable) operations 
   but with results with different format
4. extensively test the template class and its methods, 
   on signals with different formats
5. test Segmenters and fmap
--> Until here we have not tested the algorithm classes (filters / estimators)
6. test fundamental algorithms
7. develop synthetic physiological signals
8. test estimators on synthetic physiological signals
